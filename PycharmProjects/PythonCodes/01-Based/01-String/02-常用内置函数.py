"""

abs(x) --> x的绝对值
max(x)
min(x)
sum(x)

ord(s) --> 返回s的编码
chr(x) --> 返回编码为x的字符

bin(x) --> 把x转化为二进制
oct(x) --> 把x转化为八进制
int(x) --> 把x转化为十进制
hex(x) --> 把x转化为十六进制

eval(s[, globals[, locals]]) --> 计算字符串的值并返回
float(x) --> 把数字或字符串x转化为浮点数并返回
help(obj) --> 返回对象obj的帮助信息
id(obj) --> 返回obj的标识

sorted(列表,cmp[,key[reverse]]])

range(start, end, step) --> 以start开始end-1结束，每次跳过step

isdigit()--->判断是否为数字类型
isalpha()--->判断是否为字符类型
isalnum()--->判断是否为字母和数字类型
isspace()--->判断是否为纯空格


isinstance("hello", str)

"""

print(chr(1))
print(isinstance("hello", str))